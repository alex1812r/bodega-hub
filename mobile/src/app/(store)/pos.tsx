import { EmptyState, Screen } from "@/ui";

export default function PosScreen() {
  return (
    <Screen title="Punto de venta">
      <EmptyState
        description="Esta pantalla llega en la siguiente fase del plan."
        title="Punto de venta"
      />
    </Screen>
  );
}

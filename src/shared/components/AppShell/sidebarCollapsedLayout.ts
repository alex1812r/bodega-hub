/** Espaciado horizontal compartido del menu colapsado (nav, marca, pie). */
export const sidebarCollapsedGutterClassName = "px-2";

/** Celda de icono: mismas clases base que cada enlace del menu colapsado. */
export const sidebarCollapsedItemClassName =
  "flex w-full items-center justify-center rounded-lg px-2 py-3";

export const sidebarCollapsedNavClassName = `flex flex-col items-stretch gap-1 ${sidebarCollapsedGutterClassName}`;

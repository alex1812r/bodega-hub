/** Ancho estándar de ticket térmico (80 mm). */
export const RECEIPT_WIDTH_MM = 80;

/** Margen seguro para impresoras térmicas. */
export const RECEIPT_MARGIN_MM = 3;

export const RECEIPT_CONTENT_WIDTH_MM = RECEIPT_WIDTH_MM - RECEIPT_MARGIN_MM * 2;
